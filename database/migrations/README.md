# Incremental migrations

Place forward-only, reviewed SQL changes here after the canonical schema changes. Use names such as `20260904_120000_describe_change.sql`. Fresh installs use `database/schema/001_canonical.sql`; upgrades apply only migrations not recorded in `schema_migrations`.
